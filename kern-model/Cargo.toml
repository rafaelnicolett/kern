[package]
name = "kern-model"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
tokio.workspace = true
async-trait.workspace = true
thiserror.workspace = true
serde.workspace = true
serde_json.workspace = true
tracing.workspace = true
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "json"] }

# LlamaCppRuntime spawns `llama-server` via tokio::process::Command (the
# "process" feature of tokio, already enabled at the workspace level) and
# talks local HTTP with the same reqwest client — no extra crate dependency.
